{
  "name": "distress-filter",
  "version": "1.0.0",
  "description": "The Distress Filter — Motivated Seller Intelligence",
  "scripts": {
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && npm run dev",
    "install:all": "cd backend && npm install && cd ../frontend && npm install"
  }
}
